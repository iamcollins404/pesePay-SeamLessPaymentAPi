import { Transaction } from './models/transaction';
import { MakePaymentRequest, TransactionInitiationRequest } from './models/payment';
import { Currency } from './models/currency';
import { PaymentMethod } from './models/payment-method';
export declare class PesePayClient {
    private http;
    private security;
    constructor(apiKey: string, encryptionKey: string);
    initiateTransaction(request: TransactionInitiationRequest): Promise<Transaction>;
    makeSeamlessPayment(request: MakePaymentRequest): Promise<Transaction>;
    checkPaymentStatus(referenceNumber: string): Promise<Transaction>;
    getActiveCurrencies(): Promise<Currency[]>;
    getPaymentMethodsByCurrency(currencyCode: string): Promise<PaymentMethod[]>;
}
